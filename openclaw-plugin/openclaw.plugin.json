{
  "id": "cognetivy-openclaw-plugin",
  "name": "Cognetivy",
  "description": "Workflow, runs, and collections state layer for the OpenClaw agent",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "workspace": {
        "type": "string",
        "description": "Path to project root that contains .cognetivy/ (default: agent workspace or process cwd)"
      }
    }
  }
}
